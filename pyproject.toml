[project]
name = "vina-analysis-automation"
version = "0.5.0a0"
description = "TfR1 peptide Vina analysis and reproducible batch preflight"
requires-python = ">=3.12,<3.13"
dependencies = [
  "meeko==0.8.0",
  "rdkit==2026.3.6",
]

[project.optional-dependencies]
test = []

[tool.vina-analysis]
runtime-lock = "requirements-v05-lock.txt"
pymol-environment = "separate-conda-environment"
